export enum tagType {
    trip = "trip",
    admin = "admin",
    userProfile = "userProfile",
    user = "user",
    meta = "meta",
    travelBuddy = "travelBuddy"
}

export const tagTypesList = [
    tagType.trip,
    tagType.admin,
    tagType.userProfile,
    tagType.user,
    tagType.meta,
    tagType.travelBuddy


];