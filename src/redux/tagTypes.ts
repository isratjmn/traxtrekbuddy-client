export enum tagType {
    trip = "trip",
    admin = "admin",
    userProfile = "userProfile",
    user = "user",
    meta = "meta"
}

export const tagTypesList = [
    tagType.trip,
    tagType.admin,
    tagType.userProfile,
    tagType.user,
    tagType.meta


];