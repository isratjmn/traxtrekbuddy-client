import { Key } from "react";

export interface ITrip {
    id: string;
    destination: string;
    description: string;
    startDate: string;
    endDate: string;
    travelType: string;
    photos: string;
    itinerary: string;
    location: string;
}
